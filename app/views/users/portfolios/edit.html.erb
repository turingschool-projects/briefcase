<%= react_component('Portfolio', {user: @user,
  portfolio: @user.portfolio,
  avatar: @user.portfolio.avatar.url,
  slug: @user.slug,
  locations: @locations,
  userLocations: @user.portfolio.locations},
{prerender: true}) %>
