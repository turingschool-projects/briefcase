<%= react_component('AlumniShow', {currentUser: current_user,
  portfolio: @portfolio,
  user: @user, projects: @user.portfolio.projects,
  avatar: @portfolio.avatar.url,
  locations: @portfolio.locations,
  project_avatar_urls: @avatar_urls,
  resume: @portfolio.resume.url  },
{prerender: true}) %>
