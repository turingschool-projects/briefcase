<%= react_component('AlumniShow', {currentUser: current_user,
  portfolio: @user.portfolio,
  user: @user, projects: @user.projects,
  avatar: @user.portfolio.avatar.url(:regular),
  locations: @user.portfolio.locations,
  project_avatar_urls: @user.avatar_urls,
  resume: @user.portfolio.resume.url,
  markdownInfo: @user.markdown_info},
{prerender: true}) %>
